

export interface ProductTypes { 
    id: number;
    name: string;
    image: string;
    price: string;
    category: string;
}

export interface CategoryTypes {
    id: string;
    name: string;
}